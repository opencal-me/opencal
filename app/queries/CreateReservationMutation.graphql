mutation CreateReservationMutation($input: CreateReservationInput!) {
  payload: createReservation(input: $input) {
    errors {
      field
      message
    }
    reservation {
      id
    }
  }
}
