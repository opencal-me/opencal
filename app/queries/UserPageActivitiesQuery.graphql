query UserPageActivitiesQuery($userId: ID!, $timezone: String!) {
  user(id: $userId) {
    activities(timezone: $timezone) {
      ...ActivityCardActivityFragment
      id
      url
    }
    id
  }
}
