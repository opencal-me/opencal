query ActivityPageQuery($activityId: ID!) {
  activity(id: $activityId) {
    address
    coordinates {
      latitude
      longitude
    }
    descriptionHtml
    end
    id
    owner {
      avatarUrl
      id
      name
    }
    ...ReservationFooterActivityFragment
    start
    title
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
