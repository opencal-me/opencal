query ActivityPageQuery($activityId: ID!) {
  activity(id: $activityId) {
    address
    coordinates {
      latitude
      longitude
    }
    descriptionHtml
    end
    id
    name
    owner {
      avatarUrl
      id
      name
    }
    ...ReservationFooterActivityFragment
    start
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
