query UserPageQuery($userId: ID!) {
  announcement
  user(id: $userId) {
    activities {
      ...ActivityCardActivityFragment
      id
      url
    }
    avatarUrl
    firstName
    id
    initials
    isViewer
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
