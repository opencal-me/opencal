query UserPageQuery($userId: ID!) {
  announcement
  user(id: $userId) {
    activities {
      ...ActivityCardActivityFragment
      id
      joinUrl
    }
    avatarUrl
    bio
    firstName
    id
    initials
    isViewer
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
