query UserPageQuery($userId: ID!) {
  announcement
  user(id: $userId) {
    activities {
      ...ActivityCardActivityFragment
      id
      url
    }
    avatarUrl
    bio
    canEdit
    firstName
    id
    initials
    isViewer
    url
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
