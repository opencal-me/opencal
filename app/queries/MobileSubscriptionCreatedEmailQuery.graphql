query MobileSubscriptionCreatedEmailQuery($subscriptionId: ID!) {
  subscription: mobileSubscription(id: $subscriptionId) {
    id
    subject {
      firstName
      id
    }
    subscriber {
      id
      phone
    }
  }
}
